{% extends 'base.html.twig' %}

{% block title %}Détail du film{% endblock %}

{% block body %}

<div class="container bg-lighttt pt-5">

                 <div class="d-flex justify-content-around mb-4" >
                
                        <a href="{{path('backoffice_movie_list')}}" class="btn btn-success float-end">Retour</a>
                        <a  class="btn btn-success float-end" href="{{path('backoffice_movie_edit',{id : casting.id})}}">Modify</a>
                   
                </div>

    <div class="row mb-2">

        <div class="col">
            <div
                class="row g-0 border rounded overflow-hidden flex-md-row align-items-start mb-4 shadow-smmm position-relative">
                

                <div class="col-8 p-4 d-flex flex-column position-static bg-light">
                   
                   

                   

                    <dl class="row">
                        <dt class="col-sm-12">Casting Details</dt>
                        <dd class="col-sm-12">

                         {% for persons in casting.person %}
                      
                            Acteur: {{ persons.firstName }} role: {{ persons.role }}<br>
    
                             {% endfor %} 
                             
                             Movies :
                                {% for movies in casting.movie %}
                                    {{ movies.title }}, 
                                {% endfor %}
                        </dd>
                        
                    </dl>

                  

                </div>
                
            </div>

        </div>

    </div>
   

</div>
{% endblock %}
